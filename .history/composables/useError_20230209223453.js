export interface useErrorOptions {
     /**
   * Listen for document visibility change
   *
   * @default true
   */
    statusMessage: 
}
export const useError = ({ options }) => {
    return createError({

    })
}