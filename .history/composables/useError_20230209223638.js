export interface useErrorOptions {
    /**
     * Details about error
     *
     * @param true
     */
    statusMessage: String
}
export const useError = ({ options }) => {
    return createError({

    })
}