export interface useErrorOptions {
    /**
     * Details about error
     *
     * @default true
     */
    statusMessage: 
}
export const useError = ({ options }) => {
    return createError({

    })
}